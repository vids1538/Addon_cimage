name: Django CI/CD

on:
  push:
    branches: [ main ]     # change if your main branch name differs
  workflow_dispatch:        # allow manual runs

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"    # or whichever version you use locally

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run migrations
        run: |
          python manage.py migrate

      - name: Run tests
        run: |
          python manage.py test

      # Optional deploy step: uncomment when you have server secrets ready
      # - name: Deploy to server
      #   env:
      #     SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
      #     SERVER_USER: ${{ secrets.SERVER_USER }}
      #     SERVER_HOST: ${{ secrets.SERVER_HOST }}
      #   run: |
      #     echo "$SSH_PRIVATE_KEY" > private_key.pem
      #     chmod 600 private_key.pem
      #     rsync -avz -e "ssh -i private_key.pem -o StrictHostKeyChecking=no" ./ $SERVER_USER@$SERVER_HOST:/var/www/todo/
      #     ssh -i private_key.pem $SERVER_USER@$SERVER_HOST "sudo systemctl restart gunicorn"
